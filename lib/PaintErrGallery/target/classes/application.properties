# SQL dialect to default => detect per datasource
spring.jpa.database=default

# What to do to the DB (none for external; update, create, create-drop)
spring.jpa.hibernate.ddl-auto=none

spring.datasource.url=jdbc:mysql://localhost:3306/world
spring.datasource.username=imguser
spring.datasource.password=paint